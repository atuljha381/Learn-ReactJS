import React from "react";
import commentUse from "./ComponentChild";
import Clock from "./usingStates";
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'))

root.render(<Clock />)